# Kimi Brief — SEC-001
Fecha: 2026-03-27T07:46:22.982Z

## Contexto
Task: SEC-001 — Instalar shellward_mcp para AI Agent Security Middleware (8-layer defense)
Scope: security_mcp
Archivos: .agent/aurora/connectors.json, scripts/install-shellward.mjs
Owner: OPENCODE

## Plan inicial
Planeo abordar la tarea "Instalar shellward_mcp para AI Agent Security Middleware (8-layer defense)" enfocándome en security_mcp.

## Conversación Kimi
Kimi says:

## 🚨 Análisis Pre-Ejecución: SEC-001 (shellward_mcp Installation)

### 1. RIESGOS VISIBLES (High/Critical)

**R1. Ejecución de Código No Verificado (CRITICAL)**
- `scripts/install-shellward.mjs` podría contener descargas arbitrarias de binarios o scripts remotos sin verificación de checksums/firmas.
- Riesgo de supply chain attack si `shellward_mcp` no está en registry npm oficial.

**R2. Modificación de Configuración de Conectores (HIGH)**
- `.agent/aurora/connectors.json` parece ser un archivo de configuración crítica del sistema Aurora/Agent.
- Modificaciones incorrectas podrían romper el flujo actual de `kimi-task-brief` o deshabilitar conectores existentes.

**R3. Interferencia con Stack Convex (HIGH)**
- Convex maneja estado backend; un middleware de seguridad mal configurado podría bloquear llamadas legítimas a la API de Convex o exponer credenciales.

**R4. Permisos de Escritura (MEDIUM)**
- El script necesitará permisos para escribir en `.agent/aurora/` (que parece versionado) y potencialmente modificar `package.json`.

**R5. Conflicto con Prohibiciones (CRITICAL)**
- Si `install-shellward.mjs` intenta modificar `node_modules` directamente (en lugar de usar npm/yarn) o genera builds en `dist/`, violaría las restricciones del proyecto.

### 2. PASOS CRÍTICOS REQUERIDOS

1. **Verificación de Origen**: Confirmar que `shellward_mcp` es un paquete legítimo (npm registry, repo oficial, firmas).
2. **Backup Inmutable**: Copiar `connectors.json` y el estado actual de `.agent/` antes de cualquier modificación.
3. **Dry-Run del Script**: Ejecutar `install-shellward.mjs` en modo simulación primero (si soporta --dry-run).
4. **Validación de Esquema**: Verificar que la estructura JSON inyectada en `connectors.json` cumpla con el esquema esperado por `aurora-inicio.mjs`.
5. **Aislamiento de Dependencias**: Instalar en entorno de desarrollo aislado primero (no producción).
6. **Rollback Plan**: Tener comando listo para revertir cambios (`git checkout` o backup restoration).

### 3. VALIDACIONES IMPRESCINDIBLES

- [ ] **Checksum Verification**: El script debe verificar SHA-256 del paquete descargado.
- [ ] **Schema Validation**: `connectors.json` debe pasar validación JSON Schema antes de guardar.
- [ ] **No Node Modules Touch**: Verificar que el script no escriba directamente en `node_modules/` (usar solo npm/yarn/pnpm).
- [ ] **Convex Compatibility**: Confirmar que el middleware no intercepte/introspeccione llamadas `convex/react` de forma no autorizada.
- [ ] **8-Layer Config**: Validar que las 8 capas de defensa estén documentadas y no incluyan "phone home" o telemetría no deseada.
- [ ] **Git Diff Review**: Revisar manualmente el diff antes del commit (especialmente en archivos .json y .mjs).

### 4. DATOS FALTANTES (Blockers para Ejecución Segura)

**Información Crítica:**
1. **Fuente del Paquete**: ¿Es `shellward_mcp` un paquete npm público, privado, o código a clonar desde Git? ¿URL del repositorio?
2. **Versión Específica**: ¿Qué versión/tag debe instalarse? (evitar latest sin control)
3. **Estructura Actual**: No tengo visibilidad del contenido actual de `.agent/aurora/connectors.json` (¿existe? ¿está vacío?)
4. **Definición de las 8 Capas**: ¿Qué representan las 8 capas? (Input validation, Auth, Rate limiting, Audit, Encryption, etc.)
5. **Configuración de Entorno**: ¿Requiere variables de entorno específicas en `.env`? (API keys, secrets)
6. **Dependencias del Sistema**: ¿Requiere binarios del sistema (OpenSSL, libs específicas) o solo dependencias Node?
7. **Integración MCP**: ¿Es un servidor MCP (Model Context Protocol) o un middleware Express/Fetch? ¿Cómo se integra con el stack React+Vite?

**Archivos a Verificar Previamente:**
- Contenido actual de `.agent/aurora/connectors.json`
- Template o documentación de `shellward_mcp` (README, INSTALL.md)
- `package.json` actual (para verificar conflictos de dependencias)

### 5. RECOMENDACIÓN INMEDIATA

**NO PROCEDER** con la escritura de archivos hasta obtener:
- URL/documentación oficial de `shellward_mcp`
- Contenido actual de los archivos objetivo (para evaluar el delta de cambios)
- Confirmación de que el propietario (OPENCODE) ha auditado el origen del paquete

**Alternativa Segura:**
Solicitar primero el modo "dry-run" o documentación de instalación manual paso a paso antes de ejecutar scripts automatizados.

FIN KIMI