# Kimi Brief — SEC-001
Fecha: 2026-03-27T07:43:57.458Z

## Contexto
Task: SEC-001 — Instalar shellward_mcp para AI Agent Security Middleware (8-layer defense)
Scope: security_mcp
Archivos: .agent/aurora/connectors.json, scripts/install-shellward.mjs
Owner: OPENCODE

## Plan inicial
Planeo abordar la tarea "Instalar shellward_mcp para AI Agent Security Middleware (8-layer defense)" enfocándome en security_mcp.

## Conversación Kimi
Kimi says:

## Análisis Pre-Ejecución: SEC-001 — Shellward MCP Integration

### 🔴 RIESGOS VISIBLES

1. **Incompatibilidad Runtime Convex** — Shellward MCP probablemente opera como middleware Node.js tradicional, pero Convex funciona en runtime serverless/edge limitado. Las 8 capas de defensa podrían depender de APIs de Node.js no disponibles en Convex functions.

2. **Bloqueo de Pipeline de Desarrollo** — Un middleware de seguridad agresivo (8 capas) puede interceptar requests del dev server de Vite, hot reload, o llamadas a Convex durante desarrollo local, causando falsos positivos.

3. **Escalación de Privilegios en Script** — `scripts/install-shellward.mjs` podría requerir ejecución con permisos elevados o modificar archivos fuera del scope permitido (`.env`, `convex.json`, reglas de firewall).

4. **Mutación de Connectors** — `.agent/aurora/connectors.json` parece ser un registro crítico de integraciones MCP; modificarlo sin schema validation puede romper otros conectores activos (incluyendo el recién implementado Kimi Brief Pipeline).

5. **Dependencias Circulares** — Si shellward_mcp requiere acceso a Convex para logging de seguridad, pero Convex ahora requiere shellward para validación, se genera dependencia circular.

### ⚠️ PASOS CRÍTICOS

1. **Validación de Runtime** — Verificar si shellward_mcp soporta Edge Runtime (Convex) o requiere Node.js tradicional. Si es Node-only, necesitarás un adapter o proxy.

2. **Backup de Connectors** — Copiar `.agent/aurora/connectors.json` a `.agent/aurora/connectors.json.backup` antes de cualquier modificación.

3. **Schema Validation** — Asegurar que el JSON modificado pase validación contra el schema de conectores Aurora (verificar si existe `.agent/aurora/schema.json`).

4. **Rollback Plan** — Implementar en `install-shellward.mjs` una función `uninstall()` que revierta cambios si la validación falla.

5. **Integración con Task Brief** — Dado que acabas de implementar el pipeline Kimi Brief, verificar que shellward no intercepte las llamadas a `scripts/kimi-task-brief.mjs` o bloquee la escritura en `.agent/kimi/briefs/`.

### ✅ VALIDACIONES IMPRESCINDIBLES

1. **Pre-install**:
   - Verificar disponibilidad del paquete `shellward_mcp` en npm/registry
   - Revisar `package.json` actual por conflictos de versiones (especialmente `zod`, `typescript`, `vite`)
   - Validar que `AGENT_LOG` tiene permisos de escritura para registrar la instalación

2. **Durante install**:
   - Sintaxis JSON válida en `connectors.json` (usar `JSON.parse` con try-catch)
   - IDs únicos en el array de conectores (no duplicar UUIDs)
   - Paths resueltos correctamente (relative vs absolute paths en Windows `C:\Users\...`)

3. **Post-install**:
   - `npm run dev` levanta sin errores de middleware
   - Convex functions responden (test query básica)
   - Build de Vite completa exitosamente (`npm run build`)
   - Verificar que las 8 capas no bloqueen requests de `localhost:5173` (Vite default)

4. **Seguridad**:
   - Auditar `install-shellward.mjs` por ejecución de código arbitrario (eval, exec, Function constructor)
   - Verificar que no modifique `node_modules` directamente (prohibido según tus reglas)

### ❓ DATOS FALTANTES CRÍTICOS

1. **Versión específica** — ¿Qué versión de shellward_mcp? (latest es riesgoso en seguridad; necesitas versión pinned)

2. **Configuración de las 8 capas** — ¿Cuáles son? (Input validation, Rate limiting, Auth, Sanitization, Audit logging, etc.) ¿Requieren variables de entorno?

3. **Schema de connectors.json** — ¿Cuál es la estructura esperada? Ejemplo:
   ```json
   {
     "id": "shellward-sec",
     "type": "middleware",
     "layers": 8,
     "entry": "shellward_mcp"
   }
   ```

4. **Variables de entorno** — ¿Necesita `SHELLWARD_API_KEY`, `SHELLWARD_SECRET`, o certificados TLS?

5. **Integración Convex** — ¿Se instala como convex action/function o como middleware externo? ¿Requiere `convex.config.js`?

6. **Permisos de archivo** — ¿El script necesita escribir en `%APPDATA%`, registry, o solo en el proyecto?

7. **Compatibilidad con TradePortal** — ¿El proyecto actual tiene autenticación (Clerk/Auth0)? Shellward podría entrar en conflicto con middleware de auth existente.

8. **Criterios de éxito** — ¿Cómo verificas que las 8 capas están activas? ¿Hay endpoint de health check?

### 🎯 RECOMENDACIÓN INMEDIATA

Antes de escribir código, solicita al owner (OPENCODE):
- El schema exacto de `connectors.json` para Aurora
- Documentación de las 8 capas de shellward_mcp
- Variables de entorno requeridas
- Confirmación de compatibilidad Convex Edge Runtime

**No procedas con la instalación hasta tener el schema de configuración y confirmar que shellward_mcp soporta entornos serverless/edge.**

FIN KIMI