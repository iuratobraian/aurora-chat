import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tradeportal.games',
  appName: 'TradePortal Games',
  webDir: 'dist'
};

export default config;
